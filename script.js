
/*function searchTrips() {

    const departure = document.getElementById('departure-input').value;
    const arrival = document.getElementById('arrival-input').value;

    const tripFound = findTrip(departure, arrival);

    const trainIcon = document.getElementById('train-icon');
    const noResultIcon = document.getElementById('no-result-icon');

    if (tripFound) {
        trainIcon.style.display = 'block';
        noResultIcon.style.display = 'none';
    } else {
        trainIcon.style.display = 'none';
        noResultIcon.style.display = 'block';
    }
}
    */